# This file is a template, and might need editing before it works on your project.
# Official framework image. Look for the different tagged releases at:
# https://hub.docker.com/r/library/node/tags/
image: node:latest

# Docker services
# services:
#   - mongo:latest

# Cached folders
cache:
  paths:
    - node_modules/

stages:
  - test
  - deploy

before_script:
  - yarn install

test:
  stage: test
  script:
    - yarn test
  artifacts:
    paths:
      - coverage/

deploy_production:
  stage: deploy
  script:
    - echo "TODO $CI_ENVIRONMENT_URL"
  environment:
    name: production
  # only:
  #   - master
  